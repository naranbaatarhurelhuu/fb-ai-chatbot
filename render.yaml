services:
  - type: web
    name: my-chatbot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot_code.py"

git add render.yaml
git commit -m "Fix render.yaml issues"
git push origin main
